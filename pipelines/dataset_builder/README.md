# Milestone 2: dataset factory (not implemented)

Requires current purpose policy, licence/deletion checks, quality labels and source provenance;
second-pass redaction; exact/near deduplication; connected conversation/document-family splits;
benchmark decontamination; immutable signed manifests/data cards; reproducibility and deletion tests.
Do not implement row-random splits where shared sources would leak across partitions.

