# Infrastructure boundary

No cloud resources, credentials or deployments are provisioned. The health-only scaffold runs
directly with `make dev`. Before production, choose and document operational storage, event
transport, encrypted artifacts, workload identity, key management and isolated environments.
Infrastructure modules must be reviewed with the associated privacy and residency decisions.

